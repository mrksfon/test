<div class="container">
    <nav class="navbar navbar-expand-lg bg-light mb-4">
        <div class="container-fluid">
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="{{ path('app_dashboard') }}">Dashboard</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="{{ path('app_admin_user_index') }}">Users</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="{{ path('app_admin_artist_index') }}">Artists</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="{{ path('app_admin_album_index') }}">Albums</a>
                    </li>
                {% endif %}
                {% if is_granted('ROLE_USER') %}
                    {# <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="{{ path('app_admin_user_index') }}">Users</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="{{ path('app_admin_user_index') }}">Users</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="{{ path('app_admin_artist_index') }}">Artists</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="{{ path('app_admin_album_index') }}">Albums</a>
                    </li> #}
                {% endif %}
            </ul>
            {% if app.user %}
                <div class="d-flex" role="search">
                    <a class="btn btn-outline-danger" href="{{ path('app_logout') }}" type="submit">Logout</a>
                </div>
            {% else %}
                <div class="d-flex" role="search" >
                    <a class="btn btn-outline-success" href="{{ path('app_login') }}" type="submit">Login</a>
                </div>
                <div class="d-flex" role="search" class="mt-2">
                    <a class="btn btn-outline-success" href="{{ path('app_register') }}" type="submit">Register</a>
                </div>
            {% endif %}
            </div>
        </div>
    </nav>
</div>